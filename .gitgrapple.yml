---

# localHookDir: ./hooks
# autoAddHooks: ByFileName
hooks:
  - hookName: commit-msg
    files:
      - path: ./hooks/CommitMustContainValidTags.js
        extraArguments:
          - name: FilePath
            value: ./hooks/CommitMessageTags.json